#include <stdio.h>
#include "main.h"

/**
 * main - the main function
 * @argc: the counter of arguments
 * @argv: the mentioner of arguments
 * Return: returns 0 Always
 */

int main(int argc _attribute_((unused)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
