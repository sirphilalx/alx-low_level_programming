#include <stdio.h>
/**
 * print_last_digit - the function name
 * @j: the integer to be passed into the function
 * Return: returns the value of j
 */

int print_last_digit(int j)
{
	j = j % 10;

	return (j);
}
