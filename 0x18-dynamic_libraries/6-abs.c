#include "main.h"
/**
 * _abs - returns the absolute value of a number
 * @n: checks if the argument passed is a number
 * Return: always returns 0
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
	return (0);
}
