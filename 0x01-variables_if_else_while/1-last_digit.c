#include <stdlib.h>
#include <time.h>
/*
 * main - Entry point
 * Description: completing code
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	printf(strlen(n) - 1)
	return (0);
}
