#include "main.h"

/**
 * mul - the function multiplies
 * @a: the integar that is multiplied
 * @b: the multiplier
 * Return: returns the value of a multiplied by b
 */
int mul(int a, int b)
{
	return (a * b);
}
